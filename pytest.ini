[pytest]
markers =
    performance: instrumentation overhead monitoring
    reproducibility: deterministic training trajectory checks
    integration: package-level integration checks
    advisory: non-blocking diagnostic checks
